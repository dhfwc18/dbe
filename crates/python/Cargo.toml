[package]
name = "dbe-python"
version = "0.1.0"
edition = "2024"

[dependencies]
pyo3 = { version = "0.28.2", features = ["extension-module"] }
dbe = { path = "../core" }

[lib]
name = "_core"
crate-type = ["cdylib"]
