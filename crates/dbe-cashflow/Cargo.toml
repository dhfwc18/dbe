[package]
name = "dbe-cashflow"
version = "0.1.0"
edition = "2024"

[dependencies]
sobol_burley = "0.5.0"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[lib]
crate-type = ["rlib"]

[[bench]]
name = "pv_bench"
harness = false
